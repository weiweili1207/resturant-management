<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dine.mapper.DishFlavorMapper">
    <insert id="insertBatch">
        INSERT INTO sky_take_out.dish_flavor (dish_id, name, value) VALUES
        <foreach collection="dishFlavors" item="df" separator=",">
            (#{df.dishId}, #{df.name}, #{df.value})
        </foreach>
    </insert>
    <delete id="deleteBatchByDishId">
        DELETE FROM sky_take_out.dish_flavor WHERE dish_id IN
        <foreach collection="dishIds" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>
</mapper>